// This file is autogenerated by the hyperschema compiler
// Schema Version: 4
/* eslint-disable camelcase */
/* eslint-disable quotes */
/* eslint-disable space-before-function-paren */

const { c } = require('hyperschema/runtime')

const VERSION = 4

// eslint-disable-next-line no-unused-vars
let version = VERSION

// @bw/hello-request
const encoding0 = {
  preencode(state, m) {
    c.string.preencode(state, m.driver)
  },
  encode(state, m) {
    c.string.encode(state, m.driver)
  },
  decode(state) {
    const r0 = c.string.decode(state)

    return {
      driver: r0
    }
  }
}

// @bw/hello-response.strayFds
const encoding1_4 = c.array(c.string)
// @bw/hello-response.commands
const encoding1_6 = encoding1_4

// @bw/hello-response
const encoding1 = {
  preencode(state, m) {
    c.string.preencode(state, m.agent)
    c.string.preencode(state, m.platform)
    c.string.preencode(state, m.arch)
    c.uint.preencode(state, m.pid)
    state.end++ // max flag is 2 so always one byte

    if (m.strayFds) encoding1_4.preencode(state, m.strayFds)
    c.string.preencode(state, m.cwd)
    if (version >= 4 && m.commands) encoding1_6.preencode(state, m.commands)
  },
  encode(state, m) {
    const flags = (m.strayFds ? 1 : 0) | (version >= 4 && m.commands ? 2 : 0)

    c.string.encode(state, m.agent)
    c.string.encode(state, m.platform)
    c.string.encode(state, m.arch)
    c.uint.encode(state, m.pid)
    c.uint.encode(state, flags)

    if (m.strayFds) encoding1_4.encode(state, m.strayFds)
    c.string.encode(state, m.cwd)
    if (version >= 4 && m.commands) encoding1_6.encode(state, m.commands)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const r3 = c.uint.decode(state)
    const flags = c.uint.decode(state)

    return {
      agent: r0,
      platform: r1,
      arch: r2,
      pid: r3,
      strayFds: (flags & 1) !== 0 ? encoding1_4.decode(state) : null,
      cwd: c.string.decode(state),
      commands: version >= 4 && (flags & 2) !== 0 ? encoding1_6.decode(state) : null
    }
  }
}

// @bw/ping-request
const encoding2 = {
  preencode(state, m) {
    c.uint.preencode(state, m.nonce)
  },
  encode(state, m) {
    c.uint.encode(state, m.nonce)
  },
  decode(state) {
    const r0 = c.uint.decode(state)

    return {
      nonce: r0
    }
  }
}

// @bw/ping-response
const encoding3 = encoding2

// @bw/exec-request.env
const encoding4_4 = encoding1_4

// @bw/exec-request
const encoding4 = {
  preencode(state, m) {
    c.uint.preencode(state, m.kind)
    state.end++ // max flag is 64 so always one byte

    if (m.run) c.string.preencode(state, m.run)
    if (m.shell) c.string.preencode(state, m.shell)
    if (m.cwd) c.string.preencode(state, m.cwd)
    if (m.env) encoding4_4.preencode(state, m.env)
    if (m.timeoutMs) c.uint.preencode(state, m.timeoutMs)
    if (m.chunk) c.buffer.preencode(state, m.chunk)
    if (m.signal) c.string.preencode(state, m.signal)
  },
  encode(state, m) {
    const flags =
      (m.run ? 1 : 0) |
      (m.shell ? 2 : 0) |
      (m.cwd ? 4 : 0) |
      (m.env ? 8 : 0) |
      (m.timeoutMs ? 16 : 0) |
      (m.chunk ? 32 : 0) |
      (m.signal ? 64 : 0)

    c.uint.encode(state, m.kind)
    c.uint.encode(state, flags)

    if (m.run) c.string.encode(state, m.run)
    if (m.shell) c.string.encode(state, m.shell)
    if (m.cwd) c.string.encode(state, m.cwd)
    if (m.env) encoding4_4.encode(state, m.env)
    if (m.timeoutMs) c.uint.encode(state, m.timeoutMs)
    if (m.chunk) c.buffer.encode(state, m.chunk)
    if (m.signal) c.string.encode(state, m.signal)
  },
  decode(state) {
    const r0 = c.uint.decode(state)
    const flags = c.uint.decode(state)

    return {
      kind: r0,
      run: (flags & 1) !== 0 ? c.string.decode(state) : null,
      shell: (flags & 2) !== 0 ? c.string.decode(state) : null,
      cwd: (flags & 4) !== 0 ? c.string.decode(state) : null,
      env: (flags & 8) !== 0 ? encoding4_4.decode(state) : null,
      timeoutMs: (flags & 16) !== 0 ? c.uint.decode(state) : 0,
      chunk: (flags & 32) !== 0 ? c.buffer.decode(state) : null,
      signal: (flags & 64) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @bw/exec-response
const encoding5 = {
  preencode(state, m) {
    c.uint.preencode(state, m.kind)
    state.end++ // max flag is 64 so always one byte

    if (m.chunk) c.buffer.preencode(state, m.chunk)
    if (m.code) c.int.preencode(state, m.code)
    if (m.signal) c.string.preencode(state, m.signal)
    if (m.message) c.string.preencode(state, m.message)
    if (version >= 2 && m.outputsRaw) c.string.preencode(state, m.outputsRaw)
  },
  encode(state, m) {
    const flags =
      (m.chunk ? 1 : 0) |
      (m.code ? 2 : 0) |
      (m.signal ? 4 : 0) |
      (m.message ? 8 : 0) |
      (m.timedOut ? 16 : 0) |
      (version >= 2 && m.outputsRaw ? 32 : 0) |
      (version >= 2 && m.outputsTruncated ? 64 : 0)

    c.uint.encode(state, m.kind)
    c.uint.encode(state, flags)

    if (m.chunk) c.buffer.encode(state, m.chunk)
    if (m.code) c.int.encode(state, m.code)
    if (m.signal) c.string.encode(state, m.signal)
    if (m.message) c.string.encode(state, m.message)
    if (version >= 2 && m.outputsRaw) c.string.encode(state, m.outputsRaw)
  },
  decode(state) {
    const r0 = c.uint.decode(state)
    const flags = c.uint.decode(state)

    return {
      kind: r0,
      chunk: (flags & 1) !== 0 ? c.buffer.decode(state) : null,
      code: (flags & 2) !== 0 ? c.int.decode(state) : 0,
      signal: (flags & 4) !== 0 ? c.string.decode(state) : null,
      message: (flags & 8) !== 0 ? c.string.decode(state) : null,
      timedOut: (flags & 16) !== 0,
      outputsRaw: version >= 2 && (flags & 32) !== 0 ? c.string.decode(state) : null,
      outputsTruncated: version >= 2 && (flags & 64) !== 0
    }
  }
}

// @bw/put-request
const encoding6 = {
  preencode(state, m) {
    c.uint.preencode(state, m.kind)
    state.end++ // max flag is 2 so always one byte

    if (version >= 3 && m.path) c.string.preencode(state, m.path)
    if (version >= 3 && m.chunk) c.buffer.preencode(state, m.chunk)
  },
  encode(state, m) {
    const flags = (version >= 3 && m.path ? 1 : 0) | (version >= 3 && m.chunk ? 2 : 0)

    c.uint.encode(state, m.kind)
    c.uint.encode(state, flags)

    if (version >= 3 && m.path) c.string.encode(state, m.path)
    if (version >= 3 && m.chunk) c.buffer.encode(state, m.chunk)
  },
  decode(state) {
    const r0 = c.uint.decode(state)
    const flags = c.uint.decode(state)

    return {
      kind: r0,
      path: version >= 3 && (flags & 1) !== 0 ? c.string.decode(state) : null,
      chunk: version >= 3 && (flags & 2) !== 0 ? c.buffer.decode(state) : null
    }
  }
}

// @bw/put-response
const encoding7 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte

    if (version >= 3 && m.files) c.uint.preencode(state, m.files)
    if (version >= 3 && m.bytes) c.uint.preencode(state, m.bytes)
    if (version >= 3 && m.message) c.string.preencode(state, m.message)
  },
  encode(state, m) {
    const flags =
      (version >= 3 && m.ok ? 1 : 0) |
      (version >= 3 && m.files ? 2 : 0) |
      (version >= 3 && m.bytes ? 4 : 0) |
      (version >= 3 && m.message ? 8 : 0)

    c.uint.encode(state, flags)

    if (version >= 3 && m.files) c.uint.encode(state, m.files)
    if (version >= 3 && m.bytes) c.uint.encode(state, m.bytes)
    if (version >= 3 && m.message) c.string.encode(state, m.message)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      ok: version >= 3 && (flags & 1) !== 0,
      files: version >= 3 && (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      bytes: version >= 3 && (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      message: version >= 3 && (flags & 8) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @bw/get-request.globs
const encoding8_1 = encoding1_4

// @bw/get-request
const encoding8 = {
  preencode(state, m) {
    c.string.preencode(state, m.path)
    state.end++ // max flag is 1 so always one byte

    if (version >= 3 && m.globs) encoding8_1.preencode(state, m.globs)
  },
  encode(state, m) {
    const flags = version >= 3 && m.globs ? 1 : 0

    c.string.encode(state, m.path)
    c.uint.encode(state, flags)

    if (version >= 3 && m.globs) encoding8_1.encode(state, m.globs)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      path: r0,
      globs: version >= 3 && (flags & 1) !== 0 ? encoding8_1.decode(state) : null
    }
  }
}

// @bw/get-response
const encoding9 = {
  preencode(state, m) {
    c.uint.preencode(state, m.kind)
    state.end++ // max flag is 8 so always one byte

    if (version >= 3 && m.chunk) c.buffer.preencode(state, m.chunk)
    if (version >= 3 && m.message) c.string.preencode(state, m.message)
    if (version >= 3 && m.files) c.uint.preencode(state, m.files)
    if (version >= 3 && m.bytes) c.uint.preencode(state, m.bytes)
  },
  encode(state, m) {
    const flags =
      (version >= 3 && m.chunk ? 1 : 0) |
      (version >= 3 && m.message ? 2 : 0) |
      (version >= 3 && m.files ? 4 : 0) |
      (version >= 3 && m.bytes ? 8 : 0)

    c.uint.encode(state, m.kind)
    c.uint.encode(state, flags)

    if (version >= 3 && m.chunk) c.buffer.encode(state, m.chunk)
    if (version >= 3 && m.message) c.string.encode(state, m.message)
    if (version >= 3 && m.files) c.uint.encode(state, m.files)
    if (version >= 3 && m.bytes) c.uint.encode(state, m.bytes)
  },
  decode(state) {
    const r0 = c.uint.decode(state)
    const flags = c.uint.decode(state)

    return {
      kind: r0,
      chunk: version >= 3 && (flags & 1) !== 0 ? c.buffer.decode(state) : null,
      message: version >= 3 && (flags & 2) !== 0 ? c.string.decode(state) : null,
      files: version >= 3 && (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      bytes: version >= 3 && (flags & 8) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

function setVersion(v) {
  version = v
}

function encode(name, value, v = VERSION) {
  version = v
  return c.encode(getEncoding(name), value)
}

function decode(name, buffer, v = VERSION) {
  version = v
  return c.decode(getEncoding(name), buffer)
}

function getEnum(name) {
  switch (name) {
    default:
      throw new Error('Enum not found ' + name)
  }
}

function getEncoding(name) {
  switch (name) {
    case '@bw/hello-request':
      return encoding0
    case '@bw/hello-response':
      return encoding1
    case '@bw/ping-request':
      return encoding2
    case '@bw/ping-response':
      return encoding3
    case '@bw/exec-request':
      return encoding4
    case '@bw/exec-response':
      return encoding5
    case '@bw/put-request':
      return encoding6
    case '@bw/put-response':
      return encoding7
    case '@bw/get-request':
      return encoding8
    case '@bw/get-response':
      return encoding9
    default:
      throw new Error('Encoder not found ' + name)
  }
}

function getStruct(name, v = VERSION) {
  const enc = getEncoding(name)
  return {
    preencode(state, m) {
      version = v
      enc.preencode(state, m)
    },
    encode(state, m) {
      version = v
      enc.encode(state, m)
    },
    decode(state) {
      version = v
      return enc.decode(state)
    }
  }
}

const resolveStruct = getStruct // compat

module.exports = {
  resolveStruct,
  getStruct,
  getEnum,
  getEncoding,
  encode,
  decode,
  setVersion,
  version
}
