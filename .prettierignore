node_modules
coverage
package-lock.json

# Generated by `npm run build:rpc` -- formatted by that script, not by a repo-wide prettier pass.
# (Kept out of the repo-wide check so the two never fight over it.)

# The seccomp profile is generated byte-for-byte by lib/isolation/podman/seccomp.js and its
# sha256 goes into each job's attestation. Prettier must not reformat it: doing so changes the
# hash and breaks the drift guard in test/seccomp.js.
etc/seccomp/build-v1.json
